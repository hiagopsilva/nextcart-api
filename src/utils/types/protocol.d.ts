/* eslint-disable no-unused-vars */
declare namespace Protocol {
  declare type Item = {
    protocolo: string,
    apresentante: string,
    tipo: string,
    imagem: string,
    entrada: number,
    vencimento: number
  };

  declare type List = {
    id: string,
    protocolo: string,
    apresentante: string,
    tipo: string,
    imagem: string,
    entrada: number,
    vencimento: number
  };
}
